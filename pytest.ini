[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: marks tests as integration tests (require local CSV data)
    unit: marks tests as unit tests (no external dependencies)

addopts =
    --strict-markers
    -q
